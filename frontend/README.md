# LiveOS Brain Frontend v2

The modern, redesigned frontend for LiveOS Brain - a multimodal, graph-based personal memory system.

## Features

### 🏠 Landing Page
- Animated gradient background
- System overview with models and infrastructure display
- Quick navigation to all features

### 💬 Chat Interface
- Clean, modern chat UI
- LiveOS branding (no external branding)
- Real-time display of active models and systems
- Markdown rendering for responses
- Conversation history
- Smart suggestions

### 📝 Notes Interface
- **Auto-save**: Notes save automatically when you navigate away
- **No manual titles**: Titles are auto-generated by the backend during ingestion
- **Markdown preview**: Toggle between edit and preview modes
- **Smart search**: Debounced search by title and content
- **Clean UI**: Distraction-free writing experience

### 🕸️ Graph Visualization
- Interactive force-directed graph
- Color-coded node types (Concepts, Entities, Personas, Tasks, Notes)
- Click to explore node details
- Smooth animations and transitions

## Tech Stack

- **Framework**: Next.js 15 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Animations**: Framer Motion
- **Markdown**: React Markdown with remark-gfm
- **Graph**: React Force Graph 2D
- **HTTP Client**: Axios

## Getting Started

### Prerequisites
- Node.js 18+
- npm or pnpm
- Backend API running on `http://localhost:8000`

### Installation

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Build for production
npm run build

# Start production server
npm start
```

The app will be available at `http://localhost:3000`.

## Project Structure

```
src/
├── app/
│   ├── layout.tsx          # Root layout with sidebar & cursor
│   ├── page.tsx            # Landing page
│   ├── chat/
│   │   └── page.tsx        # Chat interface
│   ├── notes/
│   │   └── page.tsx        # Notes interface
│   └── graph/
│       └── page.tsx        # Graph visualization
├── components/
│   ├── sidebar.tsx         # Persistent navigation sidebar
│   ├── custom-cursor.tsx   # Custom animated cursor
│   └── grain-overlay.tsx   # Aesthetic grain texture
└── lib/
    ├── api.ts              # Backend API client
    └── utils.ts            # Utility functions
```

## Key Differences from v1

1. **Persistent Sidebar**: Always visible navigation instead of tabs
2. **Cleaner Chat**: Removed picture upload, settings, external branding
3. **Auto-save Notes**: No manual save button, automatic on blur
4. **System Info**: Display of active models and databases
5. **Modern Design**: Updated color scheme with purple/pink gradients
6. **Better UX**: Improved animations, transitions, and interactions

## Backend Integration

The frontend communicates with the FastAPI backend at `http://localhost:8000/api/v1`:

- `POST /chat` - Chat with your brain
- `GET /notes` - Fetch all notes (with optional search)
- `GET /notes/:id` - Get specific note
- `POST /ingest` - Create new note
- `PUT /notes/:id` - Update note
- `DELETE /notes/:id` - Delete note
- `GET /graph/visualization` - Get graph data

## Environment Variables

No environment variables required. The API base URL is hardcoded to `http://localhost:8000` for local development.

To change this, edit `src/lib/api.ts`:

```typescript
const API_BASE_URL = "http://localhost:8000/api/v1";
```

## Models & Systems Displayed

### AI Models
- Gemma3 12B (Chat, Extraction, Summarization)
- Whisper V3 (Audio Transcription)
- Florence 2 (Image Description)
- Qwen3 Embedding 8B (Vector Embeddings)
- MxBai Reranker (Retrieval Reranking)
- DeepSeek OCR (PDF/Image OCR)

### Infrastructure
- Postgres (Content Storage)
- Neo4j (Knowledge Graph & Vectors)
- MinIO (File Storage)

## Development

### Available Scripts

- `npm run dev` - Start development server with Turbopack
- `npm run build` - Build for production
- `npm start` - Start production server
- `npm run lint` - Run ESLint

### Code Style

- Use TypeScript for type safety
- Follow React best practices
- Use Tailwind for styling
- Prefer functional components
- Use hooks for state management

## License

Part of the LiveOS Brain project.
