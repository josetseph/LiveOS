name: Desktop release

on:
  push:
    tags:
      - "desktop-v*"
  workflow_dispatch:

env:
  ORB_PYTHON_RELEASE: "20250317"
  ORB_PYTHON_VERSION: "3.12.9"
  ORB_NODE_VERSION: "24.4.1"

jobs:
  build-mac-arm64:
    runs-on: macos-14
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install build tools (cmake for llama-cpp-python)
        run: brew install cmake || true

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Prepare distribution resources
        working-directory: desktop
        run: npm run prepare-dist

      - name: Build macOS arm64 installer (unsigned)
        working-directory: desktop
        run: npx electron-builder --mac dmg zip --arm64 -p never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload macOS arm64 artifacts
        uses: actions/upload-artifact@v6
        with:
          name: orb-mac-arm64
          path: |
            desktop/dist/*arm64*.dmg
            desktop/dist/*arm64*.zip
          if-no-files-found: error

  build-mac-x64:
    # macos-13 is retired; macos-15-intel is GitHub's remaining x64 macOS runner.
    runs-on: macos-15-intel
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install build tools
        run: brew install cmake || true

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Prepare distribution resources
        working-directory: desktop
        run: npm run prepare-dist

      - name: Build macOS x64 installer (unsigned)
        working-directory: desktop
        run: npx electron-builder --mac dmg zip --x64 -p never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload macOS x64 artifacts
        uses: actions/upload-artifact@v6
        with:
          name: orb-mac-x64
          path: |
            desktop/dist/*x64*.dmg
            desktop/dist/*x64*.zip
          if-no-files-found: error

  build-windows:
    runs-on: windows-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Prepare distribution resources
        working-directory: desktop
        run: npm run prepare-dist

      - name: Build Windows installer (unsigned)
        working-directory: desktop
        run: npx electron-builder --win --x64 -p never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v6
        with:
          name: orb-windows-x64
          path: |
            desktop/dist/*.exe
          if-no-files-found: error

  build-linux:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install build tools (cmake / compilers for llama-cpp-python)
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Prepare distribution resources
        working-directory: desktop
        run: npm run prepare-dist

      - name: Build Linux AppImage (unsigned)
        working-directory: desktop
        run: npx electron-builder --linux AppImage --x64 -p never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v6
        with:
          name: orb-linux-x64
          path: |
            desktop/dist/*.AppImage
          if-no-files-found: error
